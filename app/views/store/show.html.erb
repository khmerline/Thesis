<%= render 'layouts/nav2' %>
 
      </div>

    </div>
  </nav><br><br>

  <legend>Show product 

    <div class="float-right">
    </div><br><br><!-- line_items_path(product_id: product) (sent params [:product_id ] to line_items_path) -->
    </legend>

<div class="container">
      <ul>

          <div class="col-xs-4">
              <%= image_tag @product.image_url, :class => 'img' %>
          </div>
          <div class"col-xs-8">

             <div class="table-responsive">
  <table class="table table-striped table-hover">
    <thead class="text-">
      <tr class="text-danger">
            <th>Title</th>
           
            <th>Price</th>
            <th>Catagories</th>
            <th></th>
        <th></th>
        <th></th>
      </tr>
    </thead>

    <tbody>
      
            <td><%= @product.title %></td>
            
           
            <td><%= number_to_currency (@product.price) %></td>
            <td><%= @product.catagory.cat_type %></td>
            <td><%= button_to 'Add to cart', line_items_path(product_id: @product), :class => 'btn btn-primary btn-xs' %></td>

            
      
    </tbody>



  </table>

      <div>&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;<%= @product.read_more %></div><br><br>
</div>

  <%= link_to root_path, :class => 'btn btn-primary float-right btn-xs' do %>
       
       Back
  <% end %>