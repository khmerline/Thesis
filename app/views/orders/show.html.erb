<div class="page-header">
  <%= link_to root_path, :class => 'btn btn-warning' do %>
    <span class="glyphicon glyphicon-list-alt"></span>
    Back
  <% end %>
  
  <legend>Show order</legend>
</div>

<div class="col-xs-4">
<dl class="dl-horizontal">

  <dt>Name:</dt>
  <dd><%= @order.name %></dd>


  <dt>Address:</dt>
  <dd><%= @order.address %></dd>

  <dt>Email:</dt>
  <dd><%= @order.email %></dd>

  <dt>Pay type:</dt>
  <dd><%= @order.pay_type %></dd>
 
</div><!--------------------------- -->
  <div class="table-responsive">

  <div class="col-xs-8 pull-right">
  <table class="table table-striped table-hover">
    <thead>
      <tr>
            <th>Title</th>
            <th>Price</th>
            <th>Quanity</th>
            <th></th>

           
           
      </tr>
    </thead>

    <% current_user.orders.each do |order|%>
    <%order.line_items.each do |item| %>

    <tbody>
      
        
            <td><%= item.product.title  %></td>
            <td><%=  number_to_currency (item.product.price) %></td>
            <td> <%= item.quantity%>&times;</td>

          
          
  <% end %>
      <td class="pull-right"><%= number_to_currency(@cart.total_price)%></td>
      <dd><em><%= @order.created_at.strftime("%B %d, %Y, %A") %></em></dd>
  <dd><em><%= @order.created_at.strftime("%H:%M:%S") %></em></dd>
    </tbody>
          
   
     
     <% end %>
             
  </table>

  

  </div>
</div>


  


  

</dl>






